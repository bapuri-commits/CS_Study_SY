# Workspace Handoff — 2026-03-07

## 현재 상태
QuickDrop 프로젝트 생성 + VPS 배포 완료. `http://46.250.251.82:8200`에서 라이브 동작 중. DevOps Stage 2 실전 적용 완료 (두 번째 서비스 배포).

## 작업 내용

### QuickDrop 프로젝트 생성 및 구현
- 개인용 파일 클라우드 (카톡/메일 나에게 보내기 대체)
- FastAPI 백엔드: drop(임시 전송, 자동 만료) + vault(영구 보관, 폴더 구조)
- 반응형 웹 UI: 임시전송/보관함 탭, 검색, 이미지/비디오 썸네일, 다크모드
- CLI 클라이언트: 표준 라이브러리만 사용 (외부 의존성 없음)
- Docker 설정 (Dockerfile + docker-compose.yml)
- 워크스페이스 등록: `sync.py add QuickDrop Quick_Drop main`
- The Record `2_Projects/QuickDrop/_index.md` 생성
- GitHub 원격: https://github.com/bapuri-commits/Quick_Drop.git

### 보안 조치
- `0_Inbox/일상메모.md`에서 노출된 GitHub 토큰 제거 → revoke 필요 알림
- The Record, PrivateLLM 미커밋 파일 정리 + push

### VPS 배포 (DevOps Stage 2 적용)
- 데스크탑 SSH 키 등록 (노트북에서 SSH 접속 → authorized_keys에 추가)
- `/opt/apps/quickdrop/` — git clone + Python venv + pip install
- `/opt/envs/quickdrop.env` — 환경변수 (심링크 → .env)
- `/opt/data/quickdrop/drop/`, `/opt/data/quickdrop/vault/` — 데이터 디렉토리
- systemd 서비스 `quickdrop.service` — enabled, active (running)
- ufw 8200/tcp 개방

## DevOps VPS 현황 업데이트

### 서버 접근

| 항목 | 값 |
|------|-----|
| IP | `46.250.251.82` |
| SSH | `ssh dev@46.250.251.82` (키 인증만) |
| SSH 키 (데스크탑) | `C:\Users\user\.ssh\id_ed25519` |
| SSH 키 (노트북) | `C:\Users\chois\.ssh\id_ed25519` |

### 디렉토리 구조

```
/opt/
├── apps/
│   ├── news-agent/      ← Stage 2 배포 완료
│   └── quickdrop/       ← Stage 2 배포 완료 (NEW)
│       ├── .env → /opt/envs/quickdrop.env
│       └── .venv/
├── envs/
│   ├── news-agent.env
│   └── quickdrop.env    ← (NEW)
├── data/
│   ├── news-agent/
│   │   ├── output/
│   │   └── web/
│   └── quickdrop/       ← (NEW)
│       ├── drop/        ← 임시 파일 + .meta/
│       └── vault/       ← 영구 보관
├── logs/
├── scripts/
└── backups/
```

### systemd 서비스

| 서비스 | 포트 | 상태 |
|--------|------|------|
| quickdrop.service | 8200 | active (running), enabled |

### ufw 방화벽

| 포트 | 용도 |
|------|------|
| 22/tcp | SSH |
| 8200/tcp | QuickDrop |

### DevOps 로드맵 진행 현황

| Stage | 내용 | 상태 |
|-------|------|------|
| 1 | VPS 초기화 & 클린 세팅 | ✅ 완료 |
| 2 | 서비스 수동 배포 (News_Agent, QuickDrop) | ✅ 완료 |
| 3 | Docker 컨테이너화 (The_Agent) | 미시작 |
| 4 | nginx 리버스 프록시 | 미시작 |
| 5 | 도메인 & HTTPS | 미시작 |
| 6 | CI/CD | 미시작 |
| 7 | cron 자동화 | 미시작 |
| 8 | 로컬-VPS 하이브리드 (lesson-assist) | 미시작 |
| 9 | 모니터링 & 로깅 | 미시작 |
| 10 | DevOps-first (llm-mcp-agent) | 미시작 |
| 11 | PixelmonServer (후순위) | 미시작 |

### QuickDrop 운영 명령어

```bash
sudo systemctl status quickdrop
sudo systemctl restart quickdrop
sudo journalctl -u quickdrop -f

# 코드 업데이트
cd /opt/apps/quickdrop && git pull && sudo systemctl restart quickdrop

# 만료 파일 정리
cd /opt/apps/quickdrop/backend && /opt/apps/quickdrop/.venv/bin/python cleanup.py
```

## 핵심 결정 & 맥락
- **QuickDrop은 DevOps 학습 + 실용 겸용**: Stage 2 실전 적용 프로젝트이면서 실제 파일 전송에 사용
- **VPS 비밀번호 아직 기본값**: `changeme` → 변경 필요
- **도메인 미구매**: 현재 IP:포트로 접속. Stage 4~5에서 nginx + 도메인 + HTTPS 적용 예정
- **핸드폰 로컬 접속 불가 해결**: 데스크탑/핸드폰 서브넷 불일치 → VPS 배포로 우회 해결

## 다음 작업
- [ ] QuickDrop VPS 비밀번호 변경
- [ ] cleanup.py cron 등록
- [ ] 핸드폰에서 VPS QuickDrop 접속 테스트
- [ ] Stage 4: nginx reverse proxy 설치 + 서비스 프록시 설정
- [ ] Stage 5: 도메인 구매 + HTTPS (drop.도메인 등)

## 세션 컨텍스트
- **대화 주제**: 워크스페이스 동기화 → 전체 현황 보고 → 보안 조치 → QuickDrop 기획/구현/테스트 → VPS 배포
- **주요 논의 흐름**: sync 실행 → 액션 보고서 → 토큰 제거/미커밋 정리 → DevOps 계획 확인 → QuickDrop 설계(Plan) → MVP 구현 → vault 추가 → 웹 UI Phase 2 → VPS SSH 키 등록 → VPS 배포 성공
- **마지막 작업**: VPS에 QuickDrop 배포 완료 (systemd + ufw), 외부 접속 테스트 성공
- **참고**: VPS 마이그레이션 계획(Contabo→Hetzner+Railway)은 별개로 진행 예정. 현재는 Contabo에서 계속.
